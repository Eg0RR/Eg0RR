#include <iostream>

using namespace std;

int main()
{
    setlocale(LC_ALL, "Russian");
    float summ = 0;
    int counter = 1, a;
    cout << " Введите число = " << endl;
    cin >> a;
    while (counter <= (a * 2 - 1))
    {
        summ = summ + counter;
        counter = counter + 2;
    }
    cout << summ << endl;
 }
