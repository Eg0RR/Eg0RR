#include <iostream>
#include <ctime>
using namespace std;

int main()
{
	srand((unsigned)time(NULL));
	setlocale(LC_ALL, "Russian");
	const int N = 10;
	int a[N], i, j, n, d, x, a1, a2;
	cout << "Введите число 1 " << endl;
	cin >> a1;
	cout << "Введите число 2 " << endl;
	cin >> a2;
	a[0] = a1;
	a[1] = a2;
	for (int i = 2; i < N; i++)
		a[i] = a[i - 1] + a[i - 2];
	for (int i = 0; i < N; i++)
		cout << a[i] << endl;
}
