#include <iostream>
#include <ctime>

using namespace std;
int Sign(int x)
{
	if (x < 0)
		return -1;
	else
		if (x == 0)
			return 0;
		else
			return 1;
}
int main()
{
	setlocale(LC_ALL, "Russian");
	cout << Sign(5) + Sign(-3) << endl;
}
