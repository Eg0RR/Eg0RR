#include <iostream>
using namespace std;

int main()
{
	setlocale(LC_ALL, "Russian");
	float a = -100;
		float b = -50;
		if (a < 0)
			a = a * -1;
		if (b < 0)
			b = b * -1;
		cout << a + b << endl;
		cout << a - b << endl;
		cout << a * b << endl;
		cout << a / b << endl;
}
