#include <iostream>
#include <ctime>

using namespace std;

int main()
{
	srand((unsigned)time(NULL));
	setlocale(LC_ALL, "Russian");
	const int N = 10;
	int array[N], max, min, i, temp, step, m;
	float k = 1.247;
	for (i = 0, m = 1; i < N; i++, m = m + 2)
	{
		array[i] = m;
		cout << " array[" << i << "]  = " << array[i] << endl;
	}
}
