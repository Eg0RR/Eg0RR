#include <iostream>
#include <ctime>

using namespace std;

int main()
{
	srand((unsigned)time(NULL));
	setlocale(LC_ALL, "Russian");
	int a, b;
	cout << " Введите a = ";
	cin >> a;
	cout << " Введите b = ";
	cin >> b;
	while (a > 0)
	{
		a = a - b;
	}
	if (a < 0)
		a = a + b;
	cout << a << endl;
}
