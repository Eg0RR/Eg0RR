#include <iostream>
using namespace std;
int main()
{
    int A, B, X = 2, Y = 3;
    setlocale(LC_ALL, "Russian");
    cout << " Введите A = ";
    cin >> A;
    cout << " Введите B = ";
    cin >> B;
    if (A > X && B <= Y)
        cout << 1 << endl;
    else
        cout << 0 << endl;
}
