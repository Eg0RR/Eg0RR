using namespace std;

int main()
{
    setlocale(LC_ALL, "Russian");
    int x;
    cout << " Введите x = ";
    cin >> x;
    int y = 4 * (x - 3) * (x - 3) * (x - 3) * (x - 3) * (x - 3) * (x - 3) - 7 * (x - 3) * (x - 3) * (x - 3) + 2;
    cout << "y = " << y << endl;

}
