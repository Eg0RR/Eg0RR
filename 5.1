using namespace std;
int main()
{
	setlocale(LC_ALL, "Russian");
	float xa, xb, ya, yb;
	xa = -1;
	ya = 3;
	xb = 6;
	yb = 2;
	float AB = sqrt((xb - xa) * (xb - xa) + (yb - ya) * (yb - ya));
	cout << AB << endl;

}
