#include <iostream>
#include <ctime>

using namespace std;

int main()
{
	srand((unsigned)time(NULL));
	setlocale(LC_ALL, "Russian");
	int a, b;
	cout << " Введите a = " << endl;
	cin >> a;
	cout << " Введите b = " << endl;
	cin >> b;
	for (int i = a; i <= b; i++)
	{
		for (int k = 0; k < i; k++)
			cout << i << endl;
	}
}
