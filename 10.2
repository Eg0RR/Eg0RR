#include <iostream>
using namespace std;
int main()
{
    int A, B, C;
    setlocale(LC_ALL, "Russian");
    cout << " Введите A = ";
    cin >> A;
    cout << " Введите B = ";
    cin >> B;
    cout << " Введите C = ";
    cin >> C;
    if (A < B  && B < C)
        cout << 1 << endl;
    else
        cout << 0 << endl;
}
